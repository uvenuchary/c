#include<stdio.h>
#include<conio.h>
main()
{
char name[20];
int i,v,c;
clrscr();
printf("enter the elements");
scanf("\n enter a string");
v=c=0;
for(i=0;name[i]!='\0';i++)
{
 if(name[i]=\'a';name[i]='e';name[i]='i';
 name[i]='o';name[i]='u');
 v++;
else
c++;
}
printf("\n vowels count==%d",v);
printf("\n consonants count==%d",c);
getch();
}
